HERO COPY SWAP — Display-only text change in `src/components/marketing/rebuild/Hero.tsx`.

1. Change the `<h1>` text to the exact copy:
   "Stop losing money to missed calls, quiet quotes, and follow-ups that never happen."

2. Change the `<p>` subline text to the exact copy:
   "Most of that money slips through the gaps between the tools you already pay for — phones, website, CRM, scheduling, books. We connect those gaps so every inquiry gets answered in seconds, quiet quotes get chased, and you see what needs attention before it gets expensive — without giving anyone another system to live in."

3. Leave eyebrow, CTAs, small print, proof line, and the HeroFlow graphic untouched.

4. Verify at desktop and 390px that the H1 stays above the fold with the primary CTA and no wrapping breaks the layout.

No other files, layout, or functionality changes.